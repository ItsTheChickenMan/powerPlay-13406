package org.firstinspires.ftc.teamcode.opmodes.teleop.dance;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

import org.firstinspires.ftc.teamcode.lib.dance.BeatKeeper;
import org.firstinspires.ftc.teamcode.lib.motion.PositionableServo;
import org.firstinspires.ftc.teamcode.lib.utils.GlobalStorage;

@TeleOp(group = "zzz")
public class BeatTester extends LinearOpMode {

	@Override
	public void runOpMode() throws InterruptedException {
		/*BeatKeeper beatKeeper = new BeatKeeper(100);

		waitForStart();

		beatKeeper.restart();

		while(opModeIsActive()){
			telemetry.addData("totalBeats", beatKeeper.totalBeats());
			telemetry.update();
		}*/
	}
}
